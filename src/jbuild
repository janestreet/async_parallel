(library
 ((name async_parallel_deprecated)
  (public_name async_parallel)
  (libraries (async
              core
              sexplib))
  (preprocess (pps (ppx_jane)))))

(jbuild_version jane_street)
